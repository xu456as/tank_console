<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="me.fetonxu.tank_console.mapper.UserMapper">
    <insert id="insertUser" parameterType="me.fetonxu.tank_console.entity.User">
      INSERT INTO user(email, password, type, phone)
              VALUES (#{email}, #{password}, #{type}, #{phone});
    </insert>
    <select id="getUser" parameterType="long" resultMap="userMap">
      SELECT * FROM user WHERE id = #{userId};
    </select>

    <resultMap id="userMap" type="User">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="type" column="type" />
        <result property="phone" column="phone"/>
    </resultMap>
</mapper>
